#!/bin/sh

# set and get volume
pactl set-sink-volume @DEFAULT_SINK@ "$1" &
vol=$(pamixer --get-volume)

# send notification
dunstify -a "Volume" \
  "Volume" \
  -h int:value:$vol \
  -r 69 \
  "$vol%"
